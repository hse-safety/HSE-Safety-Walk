# HSE Safety Walk – GitHub overwrite package v133

v133 deliberately starts from a new iPhone autosave database and a new app URL so old pictures/state cannot be inherited from v132 or older versions.

Upload every file in this folder to the repository root. `app-v133.html` is a NEW file.

After GitHub Pages deploys, open this exact URL once in Safari:
https://meirum.github.io/HSE-Safety-Walk/app-v133.html?v=133

Then use Share > Add to Home Screen.

Changes:
- New IndexedDB: HSE-Safety-Walk-Draft-v133
- Legacy database cleanup
- Clear all clears the live form + active v133 draft store and saves a blank state
- Add OBS / Add Best explicitly unhide and create their cards before scrolling
- Legacy service worker/cache cleanup
